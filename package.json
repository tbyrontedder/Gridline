{
  "name": "gridline", "version": "0.1.0", "private": true, "type": "module",
  "description": "A dependency-free, WebGPU-first spreadsheet application.",
  "scripts": { "test": "node --test tests/*.test.js", "build": "node build.js", "start": "node server.js" },
  "engines": { "node": ">=20" }
}
