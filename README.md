# Gridline

An independent, Excel-inspired spreadsheet application built with plain HTML, CSS, JavaScript, and a WebGPU worksheet renderer with Canvas2D fallback.

The complete application, tests, documentation, and GitHub Pages deployment configuration are being imported from the validated Gridline 0.1.0 source bundle.
